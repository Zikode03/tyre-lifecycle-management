import { useState } from 'react';
import { CalendarDays, Car, Gauge, LogOut, ShieldCheck, Wrench, X } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { TyreTrackLogo } from '../components/brand/TyreTrackLogo';
type Task='condition'|'mileage'|'booking'|'warranty'|null;
export default function CustomerPortalPage(){const navigate=useNavigate();const [task,setTask]=useState<Task>(null);const [mileage,setMileage]=useState(68240);const [saved,setSaved]=useState('');const submit=(message:string)=>{setSaved(message);setTask(null);setTimeout(()=>setSaved(''),2200)};return <div className="min-h-screen bg-brand-canvas"><header className="border-b border-brand-line bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4 sm:px-8"><TyreTrackLogo/><button onClick={()=>navigate('/customer-access',{replace:true})} className="flex items-center gap-2 rounded-xl border border-brand-line px-3 py-2 text-sm font-semibold text-brand-muted"><LogOut size={16}/>Sign out</button></div></header><main className="mx-auto max-w-6xl px-5 py-8 sm:px-8"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-brand-orange">Customer tyre portal</p><h1 className="mt-2 text-3xl font-black tracking-tight text-brand-ink">My vehicle & tyre care</h1><p className="mt-2 text-sm text-brand-muted">Thando Mkhize · Durban Central</p></div>{saved&&<div className="mt-5 rounded-xl bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-700">{saved}</div>}<section className="mt-7 overflow-hidden rounded-[24px] bg-white ring-1 ring-black/[0.045]"><div className="flex flex-col gap-5 border-b border-black/[0.05] p-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><div className="flex h-12 w-12 items-center justify-center rounded-xl bg-[#202124] text-white"><Car size={22}/></div><div><p className="text-lg font-bold text-brand-ink">2021 Toyota Corolla</p><p className="mt-1 text-sm text-brand-muted">ND 452-871 · {mileage.toLocaleString('en-ZA')} km</p></div></div><span className="w-fit rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-bold text-emerald-700">Vehicle verified</span></div><div className="divide-y divide-black/[0.05]">{[[Gauge,'View tyre condition','See the latest verified tread readings and recommendations.','condition'],[Wrench,'Update mileage','Submit a new odometer reading between workshop visits.','mileage'],[CalendarDays,'Book tyre inspection','Choose a suitable tyre-service visit when attention is needed.','booking'],[ShieldCheck,'Warranty & protection','Review coverage for tyres fitted by the workshop.','warranty']].map(([Icon,title,desc,key])=>{const I=Icon as typeof Gauge;return <button key={String(key)} onClick={()=>setTask(key as Task)} className="flex w-full items-center justify-between gap-4 p-5 text-left hover:bg-[#FBFAF7]"><div className="flex items-center gap-3"><I size={18} className="text-brand-orange"/><div><p className="text-sm font-bold text-brand-ink">{String(title)}</p><p className="mt-1 text-xs text-zinc-400">{String(desc)}</p></div></div><span className="text-xs font-bold text-zinc-400">Open</span></button>})}</div></section><div className="mt-6 rounded-[20px] bg-orange-50 p-5 ring-1 ring-orange-100"><p className="text-sm font-bold text-brand-ink">Current recommendation</p><p className="mt-2 text-sm leading-6 text-zinc-600">Rear tyres are being monitored. TyreTrack will contact you automatically when a verified inspection or service action is due.</p></div></main>{task&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><div className="w-full max-w-lg rounded-[24px] bg-white p-5 shadow-2xl sm:p-6"><div className="flex items-center justify-between"><h2 className="text-lg font-extrabold text-brand-ink">{task==='condition'?'Tyre condition':task==='mileage'?'Update mileage':task==='booking'?'Book tyre inspection':'Warranty & protection'}</h2><button onClick={()=>setTask(null)} className="rounded-lg p-2 text-zinc-400"><X size={18}/></button></div>{task==='condition'&&<div className="mt-5 space-y-3">{[['Front left','6.2 mm','Good'],['Front right','6.0 mm','Good'],['Rear left','4.1 mm','Monitor'],['Rear right','4.3 mm','Monitor']].map(([p,t,s])=><div key={p} className="flex items-center justify-between rounded-xl bg-[#F5F4F0] p-4"><div><p className="text-sm font-bold">{p}</p><p className="mt-1 text-xs text-zinc-400">Latest verified tread</p></div><div className="text-right"><p className="font-extrabold">{t}</p><p className="text-xs text-zinc-500">{s}</p></div></div>)}</div>}{task==='mileage'&&<div className="mt-5"><label className="text-xs font-bold text-zinc-600">Current odometer<input type="number" value={mileage} onChange={e=>setMileage(Number(e.target.value))} className="mt-2 h-11 w-full rounded-xl border border-black/[0.08] px-3 text-sm"/></label><button onClick={()=>submit('Mileage update submitted.')} className="mt-5 h-11 w-full rounded-xl bg-[#202124] text-sm font-bold text-white">Submit mileage</button></div>}{task==='booking'&&<div className="mt-5 grid gap-4"><label className="text-xs font-bold text-zinc-600">Preferred date<input type="date" defaultValue="2026-08-20" className="mt-2 h-11 w-full rounded-xl border border-black/[0.08] px-3"/></label><label className="text-xs font-bold text-zinc-600">Preferred time<select className="mt-2 h-11 w-full rounded-xl border border-black/[0.08] px-3"><option>09:00</option><option>11:00</option><option>14:00</option></select></label><button onClick={()=>submit('Inspection booking request submitted.')} className="h-11 rounded-xl bg-[#202124] text-sm font-bold text-white">Request booking</button></div>}{task==='warranty'&&<div className="mt-5 rounded-xl bg-[#F5F4F0] p-4"><p className="text-sm font-bold text-brand-ink">Manufacturer warranty active</p><p className="mt-2 text-xs leading-5 text-zinc-500">Coverage depends on inspection findings. Normal wear and impact damage are not automatically treated as manufacturing defects.</p></div>}</div></div>}</div>}
