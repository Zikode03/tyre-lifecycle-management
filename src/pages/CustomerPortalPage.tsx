import { useMemo, useState } from 'react';
import { ArrowRight, CalendarDays, Camera, Car, CheckCircle2, ChevronRight, CircleGauge, Gauge, LogOut, MapPin, Phone, ShieldCheck, Sparkles, Wrench, X } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { TyreTrackLogo } from '../components/brand/TyreTrackLogo';
import { CustomerTyreInspectionFlow } from '../components/customer/CustomerTyreInspectionFlow';
import { formatBookingNumber, getNextBookingNumber, workflowStore, type DemoBooking } from '../lib/workflowStore';

type Task='condition'|'mileage'|'booking'|'warranty'|'inspect'|null;
const registration='ND 452-981';
const tyreSummary=[{position:'Front left',tread:5.2,state:'Good'},{position:'Front right',tread:4.1,state:'Attention'},{position:'Rear left',tread:5.8,state:'Good'},{position:'Rear right',tread:2.1,state:'Critical'}];

export default function CustomerPortalPage(){
 const navigate=useNavigate();const [task,setTask]=useState<Task>(null);const [mileage,setMileage]=useState(68240);const [saved,setSaved]=useState('');const [bookingDate,setBookingDate]=useState('2026-08-20');const [bookingTime,setBookingTime]=useState('09:00');
 const health=useMemo(()=>Math.round(tyreSummary.reduce((sum,t)=>sum+Math.min(100,t.tread/8*100),0)/tyreSummary.length),[]);
 const submit=(message:string)=>{setSaved(message);setTask(null);setTimeout(()=>setSaved(''),6000)};
 const requestBooking=()=>{const bookings=workflowStore.bookings();const booking:DemoBooking={id:`BKG-${Date.now()}`,bookingNumber:getNextBookingNumber(bookings,bookingDate),customer:'Thando Mkhize',registration,service:'Tyre inspection',date:bookingDate,time:bookingTime,status:'Pending'};workflowStore.saveBookings([...bookings,booking]);submit(`Booking request received. Your booking number is ${formatBookingNumber(booking)} for ${bookingDate} at ${bookingTime}.`)};
 const tasks=[{key:'inspect' as Task,icon:Camera,title:'Inspect my tyres',desc:'Use your phone camera to scan all four tyres',tone:'bg-orange-50 text-brand-orange'},{key:'condition' as Task,icon:Gauge,title:'Tyre condition',desc:'View the latest verified workshop readings',tone:'bg-emerald-50 text-emerald-700'},{key:'mileage' as Task,icon:CircleGauge,title:'Update mileage',desc:'Keep your service record accurate',tone:'bg-blue-50 text-blue-700'},{key:'booking' as Task,icon:CalendarDays,title:'Book an inspection',desc:'Choose your preferred workshop visit',tone:'bg-amber-50 text-amber-700'},{key:'warranty' as Task,icon:ShieldCheck,title:'Warranty',desc:'Check fitted-tyre protection',tone:'bg-violet-50 text-violet-700'}];
 return <div className="min-h-screen bg-[#F4F3EF] text-brand-ink"><header className="sticky top-0 z-30 border-b border-black/[0.05] bg-white/95 backdrop-blur"><div className="mx-auto flex h-[72px] max-w-7xl items-center justify-between px-5 sm:px-8"><TyreTrackLogo/><div className="flex items-center gap-3"><div className="hidden text-right sm:block"><p className="text-sm font-extrabold">Thando Mkhize</p><p className="text-xs text-zinc-400">Customer account</p></div><button onClick={()=>navigate('/customer-access',{replace:true})} className="inline-flex h-10 items-center gap-2 rounded-xl border border-black/[0.07] bg-white px-3 text-sm font-bold text-zinc-600 hover:bg-zinc-50"><LogOut size={16}/>Sign out</button></div></div></header>
 <main className="mx-auto max-w-7xl px-5 py-7 sm:px-8 sm:py-9">{saved&&<div className="mb-5 flex items-center gap-2 rounded-2xl bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-700"><CheckCircle2 size={17}/>{saved}</div>}
 <section className="relative overflow-hidden rounded-[30px] bg-[#202124] px-6 py-7 text-white sm:px-8 sm:py-8"><div className="absolute -right-16 -top-20 h-72 w-72 rounded-full border-[42px] border-white/[0.035]"/><div className="absolute -right-5 top-10 h-44 w-44 rounded-full border-[24px] border-brand-orange/10"/><div className="relative grid gap-7 lg:grid-cols-[1fr_360px] lg:items-center"><div><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.14em] text-orange-300"><Sparkles size={14}/>Your tyre care</div><h1 className="mt-3 max-w-2xl text-[32px] font-extrabold leading-tight tracking-[-.035em] sm:text-[40px]">Check your tyres before the workshop does.</h1><p className="mt-3 max-w-xl text-sm leading-6 text-zinc-300">Use your phone camera for a guided tyre check, keep your mileage current and book professional inspection when TyreTrack spots something that needs verification.</p><div className="mt-6 flex flex-wrap gap-3"><button onClick={()=>setTask('inspect')} className="inline-flex h-11 items-center gap-2 rounded-xl bg-brand-orange px-4 text-sm font-extrabold text-white"><Camera size={17}/>Inspect my tyres</button><button onClick={()=>setTask('booking')} className="inline-flex h-11 items-center gap-2 rounded-xl bg-white/10 px-4 text-sm font-bold text-white ring-1 ring-white/10">Book workshop inspection <ArrowRight size={16}/></button></div></div><div className="rounded-[24px] bg-white/[0.07] p-5 ring-1 ring-white/10"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[.12em] text-zinc-400">Verified tyre health</p><p className="mt-2 text-4xl font-extrabold">{health}%</p></div><div className="flex h-11 w-11 items-center justify-center rounded-full bg-amber-400/15 text-amber-300"><Gauge size={21}/></div></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-amber-400" style={{width:`${health}%`}}/></div><p className="mt-4 text-sm font-bold text-amber-300">Rear-right tyre requires attention</p><p className="mt-1 text-xs leading-5 text-zinc-400">Workshop measurements remain the verified source. Customer camera scans add fresh evidence between visits.</p></div></div></section>
 <div className="mt-6 grid gap-6 lg:grid-cols-[1fr_340px]"><div className="space-y-6"><section className="rounded-[26px] bg-white p-5 ring-1 ring-black/[0.045] sm:p-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-orange-50 text-brand-orange"><Car size={22}/></div><div><p className="text-xs font-bold uppercase tracking-[.12em] text-zinc-400">My vehicle</p><h2 className="mt-1 text-lg font-extrabold">2021 Toyota Corolla Cross</h2><p className="mt-1 text-sm text-zinc-500">{registration} · {mileage.toLocaleString('en-ZA')} km</p></div></div><span className="w-fit rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-bold text-emerald-700">Verified vehicle</span></div><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4">{tyreSummary.map(t=><button key={t.position} onClick={()=>setTask('condition')} className="rounded-2xl bg-[#F7F6F2] p-4 text-left transition hover:-translate-y-0.5 hover:bg-orange-50"><div className={`mb-5 h-2 w-8 rounded-full ${t.state==='Good'?'bg-emerald-500':t.state==='Critical'?'bg-red-500':'bg-amber-400'}`}/><p className="text-xs font-bold text-zinc-500">{t.position}</p><p className="mt-1 text-xl font-extrabold">{t.tread.toFixed(1)} <span className="text-xs font-bold text-zinc-400">mm</span></p><p className={`mt-2 text-xs font-bold ${t.state==='Good'?'text-emerald-700':t.state==='Critical'?'text-red-700':'text-amber-700'}`}>{t.state}</p></button>)}</div></section>
 <section><div className="mb-4"><p className="text-xs font-bold uppercase tracking-[.12em] text-zinc-400">Self service</p><h2 className="mt-1 text-lg font-extrabold">What would you like to do?</h2></div><div className="grid gap-3 sm:grid-cols-2">{tasks.map(({key,icon:Icon,title,desc,tone})=><button key={String(key)} onClick={()=>setTask(key)} className="group flex items-center gap-4 rounded-[22px] bg-white p-5 text-left ring-1 ring-black/[0.045] transition hover:-translate-y-0.5 hover:shadow-lg hover:shadow-black/5"><div className={`flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl ${tone}`}><Icon size={20}/></div><div className="min-w-0 flex-1"><p className="text-sm font-extrabold">{title}</p><p className="mt-1 text-xs text-zinc-500">{desc}</p></div><ChevronRight size={17} className="text-zinc-300 transition group-hover:translate-x-0.5 group-hover:text-brand-orange"/></button>)}</div></section></div>
 <aside className="space-y-4"><section className="rounded-[24px] bg-white p-5 ring-1 ring-black/[0.045]"><p className="text-xs font-bold uppercase tracking-[.12em] text-zinc-400">Next recommended action</p><div className="mt-4 flex h-11 w-11 items-center justify-center rounded-2xl bg-red-50 text-red-700"><Wrench size={20}/></div><h3 className="mt-4 text-lg font-extrabold">Inspect rear-right tyre</h3><p className="mt-2 text-sm leading-6 text-zinc-500">The latest verified tread is low. You can submit a fresh camera scan now, but a workshop measurement is still required for the final safety decision.</p><button onClick={()=>setTask('inspect')} className="mt-5 inline-flex items-center gap-2 text-sm font-extrabold text-brand-orange">Start camera inspection <ArrowRight size={15}/></button></section><section className="rounded-[24px] bg-[#ECEAE4] p-5"><div className="flex items-center gap-2 text-sm font-extrabold"><MapPin size={17} className="text-brand-orange"/>Durban Central</div><p className="mt-2 text-xs leading-5 text-zinc-500">Your registered TyreTrack service branch.</p><div className="mt-4 border-t border-black/[0.06] pt-4"><a href="tel:0315550180" className="inline-flex items-center gap-2 text-sm font-bold text-zinc-700"><Phone size={15}/>031 555 0180</a></div></section></aside></div></main>
 {task==='inspect'&&<CustomerTyreInspectionFlow registration={registration} onClose={()=>setTask(null)} onComplete={submit}/>} 
 {task&&task!=='inspect'&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/45 p-4 backdrop-blur-[2px]"><div className="w-full max-w-lg rounded-[26px] bg-white p-5 shadow-2xl sm:p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.12em] text-brand-orange">Customer self service</p><h2 className="mt-1 text-lg font-extrabold">{task==='condition'?'Tyre condition':task==='mileage'?'Update mileage':task==='booking'?'Book tyre inspection':'Warranty & protection'}</h2></div><button onClick={()=>setTask(null)} className="rounded-xl bg-zinc-100 p-2 text-zinc-500"><X size={18}/></button></div>{task==='condition'&&<div className="mt-5 space-y-2">{tyreSummary.map(t=><div key={t.position} className="flex items-center justify-between rounded-xl bg-[#F5F4F0] p-4"><div><p className="text-sm font-bold">{t.position}</p><p className="mt-1 text-xs text-zinc-400">Latest verified workshop tread</p></div><div className="text-right"><p className="text-sm font-extrabold">{t.tread.toFixed(1)} mm</p><p className={`mt-1 text-xs font-bold ${t.state==='Good'?'text-emerald-700':t.state==='Critical'?'text-red-700':'text-amber-700'}`}>{t.state}</p></div></div>)}</div>}{task==='mileage'&&<div className="mt-5"><label className="text-xs font-bold text-zinc-600">Current odometer<input type="number" min="1" value={mileage} onChange={e=>setMileage(Number(e.target.value))} className="mt-2 h-11 w-full rounded-xl border border-black/[0.08] px-3 text-sm outline-none focus:border-orange-300"/></label><button onClick={()=>submit('Mileage update submitted.')} className="mt-5 h-11 w-full rounded-xl bg-[#202124] text-sm font-bold text-white">Submit mileage</button></div>}{task==='booking'&&<div className="mt-5 grid gap-4"><label className="text-xs font-bold text-zinc-600">Preferred date<input type="date" value={bookingDate} onChange={e=>setBookingDate(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-black/[0.08] px-3 text-sm"/></label><label className="text-xs font-bold text-zinc-600">Preferred time<select value={bookingTime} onChange={e=>setBookingTime(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-black/[0.08] px-3 text-sm"><option>09:00</option><option>11:00</option><option>14:00</option></select></label><div className="rounded-xl bg-orange-50 p-3 text-xs leading-5 text-zinc-600">After submission, TyreTrack assigns your booking number for that date. Keep it for workshop check-in.</div><button onClick={requestBooking} className="h-11 rounded-xl bg-brand-orange text-sm font-bold text-white">Request booking</button></div>}{task==='warranty'&&<div className="mt-5 rounded-xl bg-[#F5F4F0] p-4"><div className="flex items-center gap-2 text-emerald-700"><ShieldCheck size={18}/><p className="text-sm font-extrabold">Manufacturer warranty active</p></div><p className="mt-3 text-xs leading-5 text-zinc-500">Coverage depends on inspection findings. Normal wear and impact damage are not automatically treated as manufacturing defects.</p></div>}</div></div>}
 </div>;
}
