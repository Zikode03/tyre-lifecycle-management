import { Download, Plus, Search } from 'lucide-react';
import { Link } from 'react-router-dom';
import { PageHeader } from '../components/ui/PageHeader';
import { workflowStore } from '../lib/workflowStore';
export default function CustomersPage(){const customers=workflowStore.customers();const vehicles=workflowStore.vehicles();return <div className="space-y-6"><PageHeader eyebrow="Customer management" title="Customers" description="Manage customer contact details, vehicles and tyre service relationships." action={<Link to="/customers/new" className="inline-flex h-10 items-center gap-2 rounded-xl bg-brand-orange px-4 text-sm font-bold text-white hover:bg-brand-orange-dark"><Plus size={17}/>Add customer</Link>}/><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="relative max-w-md flex-1"><Search size={17} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-zinc-400"/><input placeholder="Search name, mobile or email..." className="h-11 w-full rounded-xl border border-brand-line bg-white pl-10 pr-4 text-sm outline-none focus:border-brand-orange"/></div><button className="inline-flex h-10 items-center justify-center gap-2 rounded-xl border border-brand-line bg-white px-4 text-sm font-semibold text-brand-muted hover:bg-zinc-50"><Download size={16}/>Export</button></div><div className="overflow-hidden rounded-2xl border border-brand-line bg-white shadow-soft"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-brand-canvas text-[11px] uppercase tracking-wider text-brand-muted"><tr><th className="px-5 py-3">Customer</th><th className="px-5 py-3">Mobile</th><th className="px-5 py-3">Vehicles</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Action</th></tr></thead><tbody className="divide-y divide-brand-line">{customers.map(customer=>{const count=vehicles.filter(v=>v.customerId===customer.id).length;return <tr key={customer.id} className="hover:bg-zinc-50/70"><td className="px-5 py-4"><p className="font-semibold">{customer.name}</p><p className="mt-0.5 text-xs text-brand-muted">{customer.email}</p></td><td className="px-5 py-4">{customer.mobile}</td><td className="px-5 py-4">{count}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${customer.status==='Active'?'bg-green-50 text-green-700':'bg-zinc-100 text-zinc-600'}`}>{customer.status}</span></td><td className="px-5 py-4"><Link to={`/customers/${customer.id}`} className="text-xs font-bold text-brand-orange">Open record</Link></td></tr>})}</tbody></table></div></div></div>}
