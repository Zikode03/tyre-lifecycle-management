import { Camera, CheckCircle2, ClipboardCheck, Plus } from 'lucide-react';
import { PageHeader } from '../components/ui/PageHeader';

export default function InspectionsPage() {
  return <div className="space-y-6"><PageHeader eyebrow="Workshop operations" title="Tyre inspections" description="Capture tread depth, pressure, defects, photos and technician recommendations." action={<button className="inline-flex h-10 items-center gap-2 rounded-xl bg-brand-orange px-4 text-sm font-bold text-white"><Plus size={17}/>Start inspection</button>}/><div className="grid gap-4 md:grid-cols-3">{[['12','Completed today','Technician verified',CheckCircle2],['5','In progress','Draft inspections',ClipboardCheck],['38','Photos captured','Inspection evidence',Camera]].map(([value,label,note,Icon])=>{const I=Icon as typeof Camera;return <div key={String(label)} className="rounded-2xl border border-brand-line bg-white p-5 shadow-soft"><I size={20} className="text-brand-orange"/><p className="mt-4 text-3xl font-bold">{String(value)}</p><p className="mt-1 text-sm font-semibold">{String(label)}</p><p className="mt-1 text-xs text-brand-muted">{String(note)}</p></div>})}</div><div className="rounded-2xl border border-brand-line bg-white p-5 shadow-soft"><div className="mb-5"><h2 className="font-bold">Inspection queue</h2><p className="mt-1 text-xs text-brand-muted">Vehicles awaiting or currently undergoing tyre checks</p></div>{['ND 452-981','NU 193-442','ND 667-220'].map((registration,index)=><div key={registration} className="flex flex-col gap-3 border-t border-brand-line py-4 first:border-t-0 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold">{registration}</p><p className="mt-1 text-xs text-brand-muted">{index===0?'Toyota Corolla Cross · Thando Mkhize':index===1?'Ford Ranger · Sibusiso Dlamini':'Hyundai Creta · Priya Naidoo'}</p></div><div className="flex items-center gap-3"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${index===0?'bg-amber-50 text-amber-700':index===1?'bg-orange-50 text-brand-orange':'bg-zinc-100 text-zinc-600'}`}>{index===0?'In progress':index===1?'Awaiting technician':'Booked'}</span><button className="text-xs font-bold text-brand-orange">Open</button></div></div>)}</div></div>;
}
