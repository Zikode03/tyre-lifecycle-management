import { Car, Plus, Search } from 'lucide-react';
import { PageHeader } from '../components/ui/PageHeader';
import { vehicles } from '../data/mock';

export default function VehiclesPage() {
  return <div className="space-y-6"><PageHeader eyebrow="Vehicle registry" title="Vehicles" description="View customer vehicles, current odometer readings and active tyre sets." action={<button className="inline-flex h-10 items-center gap-2 rounded-xl bg-brand-orange px-4 text-sm font-bold text-white"><Plus size={17}/>Register vehicle</button>}/><div className="relative max-w-md"><Search size={17} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-zinc-400"/><input placeholder="Search registration, make or customer..." className="h-11 w-full rounded-xl border border-brand-line bg-white pl-10 pr-4 text-sm outline-none focus:border-brand-orange"/></div><div className="grid gap-4 lg:grid-cols-2">{vehicles.map(vehicle => <div key={vehicle.id} className="rounded-2xl border border-brand-line bg-white p-5 shadow-soft"><div className="flex items-start justify-between"><div className="flex gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-brand-canvas text-brand-orange"><Car size={20}/></div><div><p className="font-bold">{vehicle.make} {vehicle.model}</p><p className="mt-0.5 text-xs text-brand-muted">{vehicle.year} · {vehicle.customer}</p></div></div><span className="rounded-lg bg-brand-graphite px-2.5 py-1 text-xs font-bold text-white">{vehicle.registration}</span></div><div className="mt-5 grid grid-cols-3 gap-3 border-t border-brand-line pt-4 text-sm"><div><p className="text-xs text-brand-muted">Odometer</p><p className="mt-1 font-semibold">{vehicle.mileage.toLocaleString('en-ZA')} km</p></div><div><p className="text-xs text-brand-muted">Active tyres</p><p className="mt-1 font-semibold">{vehicle.activeTyres}</p></div><div><p className="text-xs text-brand-muted">Health</p><p className="mt-1 font-semibold text-green-700">Monitored</p></div></div></div>)}</div></div>;
}
