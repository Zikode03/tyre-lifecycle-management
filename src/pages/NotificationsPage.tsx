import { Bell, MessageCircleMore, Send, TriangleAlert } from 'lucide-react';
import { PageHeader } from '../components/ui/PageHeader';
export default function NotificationsPage(){return <div className="space-y-6"><PageHeader eyebrow="Customer engagement" title="Notifications" description="Track safety alerts, service reminders and customer communication."/><div className="grid gap-4 md:grid-cols-3">{[['Safety alerts','8','Require urgent customer contact',TriangleAlert,'red'],['Reminders due','34','Scheduled for today',Bell,'amber'],['Delivered','126','Messages in last 7 days',Send,'green']].map(([label,value,note,Icon,tone])=>{const I=Icon as typeof Bell; return <div key={String(label)} className="rounded-2xl border border-brand-line bg-white p-5 shadow-soft"><I size={20} className={tone==='red'?'text-red-600':tone==='amber'?'text-amber-600':'text-green-600'}/><p className="mt-4 text-3xl font-bold">{String(value)}</p><p className="mt-1 text-sm font-semibold">{String(label)}</p><p className="mt-1 text-xs text-brand-muted">{String(note)}</p></div>})}</div><div className="rounded-2xl border border-brand-line bg-white p-5 shadow-soft"><div className="flex items-center justify-between"><div><h2 className="font-bold">Communication queue</h2><p className="mt-1 text-xs text-brand-muted">Messages waiting to be sent or followed up</p></div><MessageCircleMore size={19} className="text-brand-orange"/></div><div className="mt-5 divide-y divide-brand-line">{[['Thando Mkhize','Tread recheck recommended','WhatsApp','Due now'],['Lerato Molefe','Rotation due in 500 km','SMS','Today 19:30'],['Sibusiso Dlamini','Urgent replacement assessment','WhatsApp','Due now']].map(([name,msg,channel,time])=><div key={name} className="flex flex-col gap-2 py-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold">{name}</p><p className="mt-1 text-xs text-brand-muted">{msg} · {channel}</p></div><span className="text-xs font-semibold text-brand-orange">{time}</span></div>)}</div></div></div>}
