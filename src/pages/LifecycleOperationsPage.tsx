import { useState } from 'react';
import { AlertTriangle, ArrowRightLeft, History, Recycle, RefreshCw, RotateCw } from 'lucide-react';
import { PageHeader } from '../components/ui/PageHeader';
import { createRecallCampaign, lifecycleStore, recordRemoval, recordRotation, syncOfflineDrafts, updateSupplierOrder, type SupplierOrder } from '../lib/lifecycleOperations';
import { workflowStore } from '../lib/workflowStore';

type Tab='Rotation'|'End of life'|'Recalls'|'Purchasing'|'Audit'|'Offline sync';
const tabs:Tab[]=['Rotation','End of life','Recalls','Purchasing','Audit','Offline sync'];
const input='h-11 w-full rounded-xl border border-black/[0.07] bg-white px-3 text-sm outline-none focus:border-orange-300';
export default function LifecycleOperationsPage(){
 const [tab,setTab]=useState<Tab>('Rotation');const [,setVersion]=useState(0);const vehicles=workflowStore.vehicles();const [registration,setRegistration]=useState(vehicles[0]?.registration??'');const [odometer,setOdometer]=useState(vehicles[0]?.mileage??0);const [reason,setReason]=useState('Worn below service threshold');const [destination,setDestination]=useState('Recycling');const [position,setPosition]=useState('Rear Right');const [tyreId,setTyreId]=useState('TYR-3004');const [message,setMessage]=useState('');const refresh=(msg:string)=>{setVersion(v=>v+1);setMessage(msg);setTimeout(()=>setMessage(''),2400)};
 const events=lifecycleStore.events(),recalls=lifecycleStore.recalls(),orders=lifecycleStore.orders(),audit=lifecycleStore.audit(),offline=lifecycleStore.offlineDrafts();
 const createOrder=()=>{const item:SupplierOrder={id:`PO-${Date.now()}`,supplier:'Michelin SA',branch:'Durban Central',status:'Draft',createdAt:new Date().toISOString(),expectedDate:'2026-08-22',items:[{sku:'MIC-P4-2055516',description:'Michelin Primacy 4 205/55 R16',quantity:8,unitCost:1710,received:0}]};lifecycleStore.saveOrders([item,...orders]);refresh('Draft purchase order created.')};
 return <div className="space-y-6"><PageHeader eyebrow="Lifecycle control" title="Tyre lifecycle operations" description="Manage rotation, tyre retirement, recycling, recalls, supplier replenishment, audit history and offline inspection sync."/>{message&&<div className="rounded-xl bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-700">{message}</div>}<div className="flex gap-2 overflow-x-auto rounded-2xl bg-white p-2 ring-1 ring-black/[0.05]">{tabs.map(t=><button key={t} onClick={()=>setTab(t)} className={`whitespace-nowrap rounded-xl px-4 py-2.5 text-sm font-bold ${tab===t?'bg-[#202124] text-white':'text-zinc-500 hover:bg-[#F5F4F0]'}`}>{t}</button>)}</div>
 {tab==='Rotation'&&<section className="grid gap-5 lg:grid-cols-[1fr_360px]"><div className="rounded-[24px] bg-white p-6 ring-1 ring-black/[0.05]"><div className="flex items-center gap-3"><RotateCw className="text-brand-orange"/><div><h2 className="text-lg font-extrabold">Record tyre rotation</h2><p className="text-xs text-zinc-500">Tyre identity and mileage history follow the tyre, not the wheel position.</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold">Vehicle<select value={registration} onChange={e=>setRegistration(e.target.value)} className={`mt-2 ${input}`}>{vehicles.map(v=><option key={v.id}>{v.registration}</option>)}</select></label><label className="text-xs font-bold">Odometer<input type="number" value={odometer} onChange={e=>setOdometer(Number(e.target.value))} className={`mt-2 ${input}`}/></label></div><div className="mt-5 grid grid-cols-4 gap-2">{[['FL','RL'],['FR','RR'],['RL','FL'],['RR','FR']].map(([a,b])=><div key={a} className="rounded-xl bg-[#F5F4F0] p-3 text-center"><p className="text-xs font-bold">{a}</p><ArrowRightLeft size={14} className="mx-auto my-2 text-brand-orange"/><p className="text-xs font-extrabold">{b}</p></div>)}</div><button onClick={()=>{recordRotation(registration,[{tyreId:'TYR-FL',from:'Front Left',to:'Rear Left'},{tyreId:'TYR-FR',from:'Front Right',to:'Rear Right'},{tyreId:'TYR-RL',from:'Rear Left',to:'Front Left'},{tyreId:'TYR-RR',from:'Rear Right',to:'Front Right'}],odometer);refresh('Rotation recorded.')}} className="mt-5 h-11 rounded-xl bg-brand-orange px-5 text-sm font-bold text-white">Record rotation</button></div><HistoryList events={events.filter(e=>e.type==='Rotation')}/></section>}
 {tab==='End of life'&&<section className="grid gap-5 lg:grid-cols-[1fr_360px]"><div className="rounded-[24px] bg-white p-6 ring-1 ring-black/[0.05]"><div className="flex items-center gap-3"><Recycle className="text-brand-orange"/><div><h2 className="text-lg font-extrabold">Remove tyre from service</h2><p className="text-xs text-zinc-500">Record the reason and final destination for accountability.</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold">Tyre ID<input value={tyreId} onChange={e=>setTyreId(e.target.value)} className={`mt-2 ${input}`}/></label><label className="text-xs font-bold">Vehicle<select value={registration} onChange={e=>setRegistration(e.target.value)} className={`mt-2 ${input}`}>{vehicles.map(v=><option key={v.id}>{v.registration}</option>)}</select></label><label className="text-xs font-bold">Position<select value={position} onChange={e=>setPosition(e.target.value)} className={`mt-2 ${input}`}><option>Front Left</option><option>Front Right</option><option>Rear Left</option><option>Rear Right</option></select></label><label className="text-xs font-bold">Odometer<input type="number" value={odometer} onChange={e=>setOdometer(Number(e.target.value))} className={`mt-2 ${input}`}/></label><label className="text-xs font-bold">Reason<select value={reason} onChange={e=>setReason(e.target.value)} className={`mt-2 ${input}`}><option>Worn below service threshold</option><option>Puncture damage</option><option>Sidewall damage</option><option>Warranty claim</option><option>Accident damage</option><option>Preventive replacement</option></select></label><label className="text-xs font-bold">Destination<select value={destination} onChange={e=>setDestination(e.target.value)} className={`mt-2 ${input}`}><option>Recycling</option><option>Retread</option><option>Warranty return</option><option>Stored</option><option>Scrapped</option></select></label></div><button onClick={()=>{recordRemoval({tyreId,registration,position,reason,destination,odometer});refresh('Tyre removal and destination recorded.')}} className="mt-5 h-11 rounded-xl bg-[#202124] px-5 text-sm font-bold text-white">Remove tyre</button></div><HistoryList events={events.filter(e=>e.type!=='Rotation'&&e.type!=='Fitment')}/></section>}
 {tab==='Recalls'&&<section className="rounded-[24px] bg-white ring-1 ring-black/[0.05]"><div className="flex items-center justify-between border-b p-5"><div><h2 className="text-lg font-extrabold">Manufacturer recalls</h2><p className="text-xs text-zinc-500">Campaign creation automatically matches current tyre records by manufacturer and model.</p></div><button onClick={()=>{createRecallCampaign({id:`RC-${Date.now()}`,manufacturer:'Continental',model:'UltraContact',dotRange:'DOT 2026 sample',severity:'Safety',status:'Active',action:'Inspect affected batch and replace where required.'});refresh('Recall created and affected vehicles matched.')}} className="rounded-xl bg-[#202124] px-4 py-2.5 text-sm font-bold text-white">Create matched recall</button></div>{recalls.map(r=><div key={r.id} className="grid gap-4 border-b p-5 md:grid-cols-[1fr_150px_1fr]"><div><p className="flex items-center gap-2 font-extrabold"><AlertTriangle size={16} className="text-brand-orange"/>{r.manufacturer} {r.model}</p><p className="text-xs text-zinc-400">{r.id} · {r.dotRange}</p></div><div><p className="text-xs text-zinc-400">Affected</p><p className="font-extrabold">{r.affectedTyres} tyres</p></div><div><p className="text-sm font-bold">{r.action}</p><p className="text-xs text-zinc-400">{r.matchedVehicles.join(', ')||'No vehicles matched'}</p></div></div>)}</section>}
 {tab==='Purchasing'&&<section className="rounded-[24px] bg-white ring-1 ring-black/[0.05]"><div className="flex items-center justify-between border-b p-5"><div><h2 className="text-lg font-extrabold">Supplier purchasing & replenishment</h2><p className="text-xs text-zinc-500">Create, submit and receive purchase orders.</p></div><button onClick={createOrder} className="rounded-xl bg-brand-orange px-4 py-2.5 text-sm font-bold text-white">Create PO</button></div>{orders.map(o=><div key={o.id} className="grid gap-4 border-b p-5 lg:grid-cols-[150px_1fr_160px_240px]"><div><p className="font-extrabold">{o.id}</p><p className="text-xs text-zinc-400">{o.status}</p></div><div><p className="font-bold">{o.supplier}</p><p className="text-xs text-zinc-400">{o.items.map(i=>`${i.received}/${i.quantity} received · ${i.description}`).join(', ')}</p></div><div><p className="text-xs text-zinc-400">Expected</p><p className="font-bold">{o.expectedDate}</p></div><div className="flex flex-wrap gap-2">{o.status==='Draft'&&<button onClick={()=>{updateSupplierOrder(o.id,'Submitted');refresh('Purchase order submitted.')}} className="rounded-lg bg-[#202124] px-3 py-2 text-xs font-bold text-white">Submit</button>}{['Submitted','Partially received'].includes(o.status)&&<button onClick={()=>{updateSupplierOrder(o.id,'Received',true);refresh('Purchase order fully received.')}} className="rounded-lg bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-700">Receive all</button>}<button onClick={()=>{updateSupplierOrder(o.id,'Cancelled');refresh('Purchase order cancelled.')}} className="rounded-lg bg-zinc-100 px-3 py-2 text-xs font-bold">Cancel</button></div></div>)}</section>}
 {tab==='Audit'&&<section className="rounded-[24px] bg-white ring-1 ring-black/[0.05]"><div className="border-b p-5"><h2 className="text-lg font-extrabold">Audit trail</h2><p className="text-xs text-zinc-500">Every important prototype action is appended with actor, time, entity and detail.</p></div>{audit.map(a=><div key={a.id} className="grid gap-3 border-b p-5 md:grid-cols-[190px_190px_1fr]"><div><p className="text-sm font-bold">{new Date(a.at).toLocaleString()}</p><p className="text-xs text-zinc-400">{a.actor}</p></div><div><p className="text-sm font-extrabold">{a.action}</p><p className="text-xs text-zinc-400">{a.entityType} · {a.entityId}</p></div><p className="text-sm text-zinc-600">{a.details}</p></div>)}</section>}
 {tab==='Offline sync'&&<section className="rounded-[24px] bg-white p-6 ring-1 ring-black/[0.05]"><div className="flex items-center justify-between gap-4"><div className="flex gap-3"><RefreshCw className="text-brand-orange"/><div><h2 className="text-lg font-extrabold">Offline inspection queue</h2><p className="text-xs text-zinc-500">Local drafts remain available until connectivity returns.</p></div></div><button onClick={()=>{syncOfflineDrafts();refresh('Offline drafts synced.')}} className="rounded-xl bg-[#202124] px-4 py-2.5 text-sm font-bold text-white">Sync now</button></div><div className="mt-5 divide-y">{offline.length?offline.map(d=><div key={d.id} className="flex items-center justify-between py-4"><div><p className="font-extrabold">{d.registration}</p><p className="text-xs text-zinc-400">{d.id} · {new Date(d.updatedAt).toLocaleString()}</p></div><span className="rounded-full bg-[#F5F4F0] px-3 py-1.5 text-xs font-bold">{d.status}</span></div>):<p className="py-10 text-center text-sm text-zinc-400">No offline drafts.</p>}</div></section>}
 </div>;
}
function HistoryList({events}:{events:ReturnType<typeof lifecycleStore.events>}){return <aside className="rounded-[24px] bg-[#202124] p-5 text-white"><div className="flex items-center gap-2"><History size={18} className="text-orange-300"/><h3 className="font-extrabold">Recent lifecycle events</h3></div><div className="mt-4 space-y-3">{events.slice(0,6).map(e=><div key={e.id} className="rounded-xl bg-white/[0.06] p-3"><p className="text-sm font-bold">{e.type} · {e.tyreId}</p><p className="mt-1 text-xs text-zinc-400">{e.registration}{e.position?` · ${e.position}`:''}</p><p className="mt-1 text-xs text-zinc-500">{e.reason??e.notes??'Lifecycle event recorded'}</p></div>)}</div></aside>}
