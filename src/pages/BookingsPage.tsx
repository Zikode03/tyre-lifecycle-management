import { CalendarDays, ChevronLeft, ChevronRight, Plus } from 'lucide-react';
import { Link } from 'react-router-dom';
import { PageHeader } from '../components/ui/PageHeader';
import { workflowStore } from '../lib/workflowStore';
export default function BookingsPage(){const bookings=workflowStore.bookings();return <div className="space-y-7"><PageHeader eyebrow="Workshop schedule" title="Bookings" description="Plan tyre-service visits and move checked-in vehicles into inspection." action={<Link to="/bookings/new" className="inline-flex h-11 items-center gap-2 rounded-xl bg-[#202124] px-4 text-sm font-bold text-white"><Plus size={16}/>New booking</Link>}/><section className="overflow-hidden rounded-[26px] bg-white ring-1 ring-black/[0.045]"><div className="flex flex-col gap-4 border-b border-black/[0.05] p-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-orange-50 text-brand-orange"><CalendarDays size={18}/></div><div><h2 className="text-base font-black text-brand-ink">Monday, 17 August 2026</h2><p className="mt-1 text-xs text-zinc-400">Workshop appointment schedule</p></div></div><div className="flex items-center gap-1"><button className="flex h-9 w-9 items-center justify-center rounded-lg bg-[#F5F4F0] text-zinc-500"><ChevronLeft size={15}/></button><button className="h-9 rounded-lg bg-[#F5F4F0] px-3 text-xs font-bold text-zinc-600">Today</button><button className="flex h-9 w-9 items-center justify-center rounded-lg bg-[#F5F4F0] text-zinc-500"><ChevronRight size={15}/></button></div></div><div className="divide-y divide-black/[0.045]">{bookings.map(item=><Link to={`/bookings/${item.id}`} key={item.id} className="flex flex-col gap-3 p-5 text-left transition hover:bg-[#FBFAF7] sm:flex-row sm:items-center sm:justify-between"><div className="flex min-w-0 gap-4"><div className="w-14 shrink-0 text-sm font-black text-brand-orange">{item.time}</div><div><p className="text-sm font-black text-brand-ink">{item.customer}</p><p className="mt-1 text-xs text-zinc-500">{item.registration} · {item.service}</p></div></div><div className="flex items-center gap-3"><span className={`w-fit rounded-full px-2.5 py-1 text-[10px] font-black ${item.status==='Checked in'?'bg-emerald-50 text-emerald-700':item.status==='Pending'?'bg-amber-50 text-amber-700':item.status==='Cancelled'?'bg-red-50 text-red-700':'bg-zinc-100 text-zinc-600'}`}>{item.status}</span><span className="text-xs font-bold text-zinc-400">Open</span></div></Link>)}</div></section></div>}
